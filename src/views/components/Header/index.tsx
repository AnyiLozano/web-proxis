import React, { FC } from "react";

const Header : FC = () : JSX.Element => {
    return (
        <React.Fragment>
            <span>Hola Soy El Header!</span>
        </React.Fragment>
    );
}

export default Header;